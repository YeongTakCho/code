import queue
import sys; read= sys.stdin.readline

N,M = map(int,read().strip().split())
s= [list(map(int,read().strip())) for _ in range(N)]
shortestRoute= [[[0,0]] * M] * N

def bfs(start,stack,s):
    n_stack = []
    for 
    
    bfs(start+1, n_stack,s)


queue 
bfs(1,stack,s)

for line in shortestRoute:
    print(line)